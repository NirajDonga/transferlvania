# Server Configuration
PORT=4000

# Database (REQUIRED)
# PostgreSQL connection string from Neon, Supabase, or local instance
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Encryption Key (REQUIRED)
# Protects file metadata (names, types) in database using AES-256-GCM
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be at least 32 characters, recommended 64 characters
# WARNING: Never use the default dev key in production!
ENCRYPTION_KEY="dev-only-insecure-key-change-in-production-minimum-32-chars-required"

# Environment
NODE_ENV="development"

# TURN Server (Optional - improves connectivity for users behind strict NATs)
# Leave empty to use Google's public STUN server only
# TURN_SERVER="turn.your-domain.com"
# TURN_SECRET="your-turn-server-shared-secret"
# TURNS_ENABLED="true"
